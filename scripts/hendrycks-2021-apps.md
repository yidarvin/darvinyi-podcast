---
slug: hendrycks-2021-apps
title: "Measuring Coding Challenge Competence With APPS"
description: "Ten thousand real coding-challenge problems, graded the way a judging site grades a human: by running the code. The best 2021 model still solved under four percent of the easy ones."
date: 2026-07-20
guest_name: "Wren"
guest_voice: "af_nicole"
---
[S] Zero point zero zero. That is the competition-level strict accuracy for every single model in today's paper, on the benchmark's hardest tier.
[O] And on the easiest tier, introductory problems a first-year computer science student solves in minutes, the best system here still cracked under four percent of them.
[S] So depending which of those two numbers you quote, this paper either says language models can now code, or language models basically still cannot. I know which one I believe.
[O] Both readings are on the table, and untangling them is the whole episode. Welcome to Litsearch Audio, where an optimist, a skeptic, and a guest scholar take apart one paper from the litsearch atlas. Today's paper is Measuring Coding Challenge Competence With APPS, by Dan Hendrycks, Steven Basart, Saurav Kadavath, and colleagues at U C Berkeley, the University of Chicago, U I U C, and Cornell, presented at NeurIPS twenty twenty-one's Datasets and Benchmarks track.
[S] Joining us is Wren, a researcher who has studied this benchmark closely. Wren, welcome.
[G] Thanks for having me. APPS is worth revisiting because it quietly set the template every code-generation benchmark since has copied, even though the numbers it reported in twenty twenty-one look almost primitive by today's standards.
[O] Wren, set the scene for us. What was broken about code-generation evaluation before this paper?
[G] Before APPS, the field had two kinds of benchmarks, and both dodged the hard part of programming. CONCODE generated Java from docstrings, SPoC converted line-by-line pseudocode into C plus plus, and CodeXGLUE aggregated similar tasks. In every case, the model started from something already close to code.
[S] And how was that graded?
[G] With text-overlap metrics, mostly BLEU, borrowed straight from machine translation. The docstrings in the Java benchmark averaged just fourteen point seven words, things like convert mixed case to underscores. That is barely a specification.
[O] So the model's job there was closer to translation than to invention.
[G] Right, and the paper argues real programming is different along two axes at once. The input should be an open-ended natural-language specification, long, sometimes with mathematical notation and edge cases, not a docstring or pseudocode. And the output should be judged by whether it actually runs correctly, not by how many words it shares with a reference solution.
[S] That second point is the one I care about most. BLEU has no idea whether code even compiles.
[G] It does not, and the paper eventually demonstrates that directly. BLEU can rank a weaker model above a stronger one. We will get to that.
[O] Walk us through what APPS actually is, then.
[G] Ten thousand programming problems, manually curated from open coding-challenge sites, with Codewars, AtCoder, Kattis, and Codeforces named as the main sources. Each problem carries a natural-language specification, and the average problem runs two hundred ninety-three point two words, roughly twenty times longer than those Java docstrings.
[S] Two hundred ninety-three words. That is a real spec, not a one-liner.
[G] It ships with a lot of scaffolding to make grading rigorous. One hundred thirty-one thousand seven hundred seventy-seven test cases in total, and two hundred thirty-two thousand four hundred twenty-one human-written reference solutions, which the paper says is over an order of magnitude more than comparable datasets. The data splits evenly, five thousand training problems and five thousand test problems, and every test problem carries an average of twenty-one point two test cases.
[O] And this was not automated scraping and done. Graduate and undergraduate authors spent six months refining it.
[G] Custom HTML parsers per source, so LaTeX and formatting survived intact. The MathPix API converted equation images into LaTeX. Any problem depending on a figure got dropped. And they deduplicated with tf-idf features plus SVD dimensionality reduction and cosine similarity, to kill near-duplicate problems. It is a genuinely curated dataset, not a dump.
[S] What stops a model from pattern-matching its way through instead of actually solving anything?
[G] That is where the three difficulty tiers come in. Introductory problems are things a programmer with one to two years of experience solves without needing sophisticated algorithms, like counting vowels in a string. Three thousand six hundred thirty-nine of those exist, with one thousand held out for test. Interview-level problems need real data structures and nontrivial algorithms, the kind you would get asked in a technical interview, five thousand total, three thousand in test. Competition-level problems sit at USACO, IOI, and ACM contest difficulty, one thousand three hundred sixty-one total, one thousand in test.
[O] So instead of one blurry number, you get three honest ones.
[G] Exactly, and that turns out to matter enormously once you see the results.
[S] How does grading actually work?
[G] By execution, the same way a judging site grades a human. It reports two metrics. Test Case Average is the mean fraction of a problem's test cases a submission passes, averaged across problems, a lenient signal that still credits code handling the common case while missing a corner. Strict Accuracy is stricter: every single test case has to pass for a problem to count as solved.
[O] So Strict Accuracy is the actually-got-the-job metric.
[G] Right, and the paper is explicit that Strict Accuracy is meant to be the eventual gold standard, with Test Case Average serving as an interim signal while models are still weak enough that strict accuracy alone would show almost nothing.
[S] Which models did they actually run?
[G] Four. GPT-2 at zero point one billion and one point five billion parameters, fine-tuned. GPT-Neo at two point seven billion parameters, fine-tuned. And GPT-3 at one hundred seventy-five billion parameters, evaluated few-shot only, because its weights were not public, so it could not be fine-tuned.
[O] GPT-2 was not originally trained on code, right?
[G] Correct, so the authors first further-pretrained it on thirty gigabytes of Python scraped from GitHub repositories with more than one star, and they decontaminated that pretraining corpus, removing any repository matching common exercise keywords or sharing function signatures with APPS starter code. GPT-Neo, by contrast, came pretrained on the Pile, which includes GitHub, and that GitHub slice never went through the same decontamination process.
[S] Hold that thought, because that asymmetry is going to matter in a few minutes.
[O] Let us get to the numbers. What did the best model actually achieve?
[G] Fine-tuned GPT-Neo two point seven billion was the strongest system tested. On introductory problems it passed fourteen point six eight percent of test cases on average, and reached three point nine zero percent strict accuracy, meaning it fully solved roughly one in twenty-five introductory problems. On competition problems, every single model scored zero point zero zero percent strict accuracy.
[S] Zero. Not close to zero. Zero.
[G] Zero, across the board, on the hardest tier. That is a floor effect. The benchmark produced no usable signal there in twenty twenty-one.
[O] But the trend across the easier tiers is genuinely upward. Accuracy improves with fine-tuning and with model scale, and the jump from GPT-2 one point five billion to GPT-Neo two point seven billion is bigger than the jump from GPT-2 zero point one billion up to one point five billion.
[G] There is a second axis of improvement that may matter more than accuracy itself, and that is syntax errors. On introductory problems, roughly fifty-nine percent of GPT-3's generated solutions had outright syntax errors, code that would not even parse. GPT-Neo's rate was about three percent. The paper frames this as models learning to write code that at least runs, decreasing exponentially as capability improves.
[S] That GPT-3 comparison is the most counter-intuitive result to me, a one hundred seventy-five billion parameter model getting beaten by something two orders of magnitude smaller.
[G] It happened. GPT-3 passed just zero point five seven percent of introductory test cases and solved only three problems out of five thousand outright, two introductory and one interview. Part of that is the syntax-error rate I just mentioned. A program that will not parse cannot pass any test case, however sound its logic is underneath.
[O] Sampling more attempts helps a lot too, I take it.
[G] Considerably. With beam search width five, evaluating all five beams instead of just the top one lifts GPT-Neo's introductory test-case average from fourteen point seven percent up to nineteen point nine percent, and strict accuracy from three point nine up to five point five percent. The paper's own framing is that the strongest model passes almost twenty percent of test cases given five attempts.
[S] And you promised BLEU would come back.
[G] It does, and this is one of the sharper results in the paper. BLEU actually rises with problem difficulty even as real accuracy falls, and it ranks GPT-2 zero point one billion, with BLEU scores of twenty-six point eight, twenty-nine point seven, and thirty point two across the three tiers, above the far stronger GPT-Neo two point seven billion, whose BLEU scores are twenty-seven point one, twenty-nine point one, and twenty-nine point three. BLEU says the weaker model is better. That is exactly backwards.
[O] That is a direct demonstration the old metric was actively misleading, not just imprecise.
[G] The paper also points at where this was heading next. It cites the contemporaneous Codex, a twelve billion parameter model from OpenAI, which on these same introductory problems reached four point one four percent top-one accuracy and nine point six five percent top-five, and twenty-five percent once sampling scaled up to one thousand attempts. Better, but still nowhere near solved.
[O] Okay, let me make the optimist case. This paper captured a phase transition in real time. Models went from writing code that mostly does not parse to code that solves real, if simple, problems, within one paper's worth of scaling from GPT-2 to GPT-Neo. The syntax-error curve alone tells you the mechanical barriers were falling fast, and Codex's follow-up numbers show the scaling kept going right on schedule.
[S] Here is my deflationary case. The paper's own headline comparison, small fine-tuned models beating a giant few-shot GPT-3, is apples to oranges by construction. GPT-3 was never fine-tuned because its weights were closed, so nobody actually knows what a fine-tuned GPT-3 would have scored. And a real chunk of that gap is arguably a formatting bug, syntax errors killing otherwise-valid logic before it even gets graded.
[G] Both points are fair, and the paper does not hide either one. It explicitly notes that GPT-3's improper formatting caused syntax errors on otherwise functioning code. So that two-orders-of-magnitude framing does oversell what was actually tested.
[O] Fine, I will take that point. It does not erase the syntax-error trend itself, though, which holds across the fine-tuned models independent of the GPT-3 comparison.
[S] Agreed, that part survives. My second objection is contamination. GPT-Neo, the best model, was pretrained on Pile data that was never decontaminated against APPS, while GPT-2's pretraining corpus was. The winning model had a data-cleanliness advantage its competitors did not have.
[G] The authors anticipate that objection and argue against pure memorization, since performance tracks difficulty level rather than being flat across tiers, which is what you would expect from real problem-solving rather than lookup. That is a genuinely reasonable argument. It does not fully close the gap just raised, though. It shows memorization is not the whole story, not that the decontamination asymmetry contributed nothing.
[O] So call that a partial win for the skeptic side.
[S] I will take it. Third point, and I think it is underrated. Test Case Average flatters the results. Passing fifteen to twenty percent of test cases reads like real progress. Strict accuracy, actually solving the whole problem, is three point nine to five point five percent on the easiest tier, and flat zero on Competition. That is a very different headline.
[G] That is accurate, and there is one more soft spot worth adding, inside the strict metric itself. Among problems with only two test cases that GPT-Neo passed, eight of twelve were false positives, because small output spaces like yes or no let a wrong program guess correctly. The paper bounds the overall false-positive rate under one percent, since most problems carry far more tests, and it is upfront about the leak, but it is a real asterisk on the gold-standard number.
[O] Where I still land, though, is that none of those are reasons to distrust the instrument. They are reasons to distrust one paper's early read of the instrument. The three-tier structure, the execution grading, the sheer scale of the test bank, that design is what let every one of those objections even get stated this precisely. A blurrier benchmark would not have exposed its own weaknesses so clearly.
[S] That is the strongest thing you have said all episode, and I do not disagree. The benchmark design holds up. My skepticism is entirely about how much weight the twenty twenty-one numbers themselves can carry.
[O] So Wren, what actually changed downstream because of this paper?
[G] It set the template. Execution-graded, difficulty-tiered evaluation from long natural-language specifications became the default for code benchmarks that followed, with a lineage that runs through SWE-bench, BigCodeBench, and the Code Llama line of work. That is arguably the paper's more durable contribution, not a specific accuracy number, but the evaluation methodology itself.
[S] It is also a clean case study for anyone doing evaluation design more broadly. The BLEU-versus-execution comparison is a textbook example of a proxy metric actively pointing the wrong direction.
[O] And a reminder that difficulty tiering, not one aggregate score, is what actually reveals where a model's competence ends.
[G] If there is one sentence to take from this paper, it is that APPS proved functional correctness could be measured directly, at scale, and that doing so immediately overturned what the old text-overlap metric had been telling everyone.
[O] Mine is that this is the paper that caught language models right as they crossed from cannot write code to starting to, and you can watch it happen in the syntax-error chart alone.
[S] Mine is to read the strict-accuracy column, not the test-case-average column, before believing any code-generation headline. This paper is the reason we know to check.
[O] That is Litsearch Audio. Full figures, results tables, and the eval critique are in the writeup on the litsearch site. Thanks for listening.
