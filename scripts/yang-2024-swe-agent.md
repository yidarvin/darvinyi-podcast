---
slug: yang-2024-swe-agent
title: "SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering"
description: "A frozen GPT-4 Turbo can't reliably edit a file in a bare Linux shell, but wrap it in a purpose-built agent-computer interface and its real-GitHub-issue resolution rate nearly doubles over the best interactive baseline."
date: 2026-07-19
guest_name: "Marcus"
guest_voice: "bm_lewis"
---
[O] Here's the claim that stopped me cold: take the exact same GPT-4 Turbo, don't touch a single weight, and just by changing how it talks to a computer, its real-world bug-fixing rate on the hardest split goes from about one percent to eighteen percent.
[S] And here's my read on that: a big chunk of that jump is beating a strawman. The fair, apples-to-apples comparison, model held fixed, interactive agent versus interactive agent, is eleven percent to eighteen percent. Real, but a lot less dramatic than "the interface changes everything."
[O] Sure, but eleven to eighteen is still a sixty-four percent relative gain from redesigning nothing but the commands and the feedback. That's the entire thesis of this paper: the interface is a first-class lever, as real as swapping in a better model.
[S] I'll grant it's real. I just want to know exactly which design choices earned that gain, and which ones are dressing.
[O] Welcome to Litsearch Audio, where an optimist, a skeptic, and a guest scholar take apart one paper from the litsearch site. Today's paper is S-W-E-agent: Agent-Computer Interfaces Enable Automated Software Engineering, by John Yang, Carlos Jimenez, and colleagues at Princeton, published at NeurIPS in twenty twenty-four.
[S] It's on the docket because it's the paper that gave the coding-agent world a vocabulary word, "agent-computer interface," and a working open-source system that's still a reference point two years later.
[O] To help us dig in is Marcus, who's spent real time with this paper. Marcus, welcome.
[G] Thanks for having me. Usual disclaimer up front: I'm not one of the seven authors, so when I say "they found" or "the paper shows," that's me reporting, not claiming credit.
[S] Marcus, start with the gap. Why did anyone need a special interface for a language model in the first place?
[G] Because by early twenty twenty-four, the default way to let a language model act was just to hand it a raw Linux shell and let it type commands. The paper's own framing is blunt: a model in a bare shell "fails to provide simple commands to edit a small file segment, and does not provide any feedback if the user makes an invalid edit." It'll cat out a three-thousand-line file to find four lines it actually needs, and that flood of irrelevant text doesn't just waste tokens, it measurably hurts the model, because distracting context is known to degrade performance.
[O] Which is such a human-computer-interaction idea dressed up as a machine-learning result. Nobody hands a new engineer a terminal with no editor, no linter, no search, and calls that a fair test of their ability.
[G] That's precisely the analogy the authors lean on. Humans get VSCode or PyCharm, tools built for how humans think. A bare shell is an interface built with a human or a program in mind, not this third thing: an LM agent. Their claim is that an LM agent is a genuinely new category of end user, with its own needs and abilities, and deserves an interface designed for it, the same way a code editor is designed for a person.
[S] And the stakes weren't abstract. The companion benchmark, S-W-E-bench, had just shown how bad things were: ask a model to resolve a real GitHub issue from one of twelve popular Python repositories, grade it with the project's own unit tests, and the original non-interactive, retrieval-augmented system resolved under four percent of them. Repository-level software engineering was wide open.
[G] Right, and prior work had poked at pieces of this puzzle, tool use, prompting tricks, execution feedback, always in isolation. Nobody had sat down and asked: what if the interface itself is the variable everyone's been ignoring?
[O] So walk us through what they actually built.
[G] The core object is what they call the agent-computer interface, or A-C-I: a thin layer that defines two things, exactly which commands the agent is allowed to issue, and exactly how the environment reports back what happened. S-W-E-agent runs in the style of ReAct, so each turn the model writes a thought and then a command, reads the result, and goes again. On top of a plain shell, they add four purpose-built pieces.
[S] Let's take them one at a time, because I think the details matter more than the headline number.
[G] First, search and navigation: find_file, search_file, search_dir. Instead of dumping every match, they return a compact summary, and if a query pulls back more than fifty results, the agent doesn't get a wall of text, it's told to write a narrower query. Second, the file viewer: the open command shows at most one hundred lines at a time with line numbers, and it always tells the agent the file's total length and how many lines are hidden above and below the current window. Third, the file editor: one edit command replaces a line range in a single shot and immediately redisplays the updated file, so the agent sees the result of its own change without issuing a second command.
[O] And that editor has a linter built in, right? That's the piece that stuck with me.
[G] It is, and it's arguably the cleverest single choice in the paper. Every edit is checked by a code linter before it's applied. If the edit would introduce a syntax error, it's rejected, the agent is shown the before and after and asked to try again. Nothing broken ever actually lands in the file. Fourth is context management: anything older than the last five observations collapses to a single line, and repeated error messages get suppressed, so the window stays focused on the current plan instead of stale file dumps.
[S] How much of this was principled design versus trial and error?
[G] Honestly, both. They describe four design principles: actions should be simple, actions should be compact and consolidate multi-step work into one call, feedback should be informative but concise, and guardrails should catch and shorten error spirals. But the actual tuning process was two cheap loops on a development split, manually reading agent trajectories to spot pain points, and a grid search over window size, history handling, and decoding temperature. No fine-tuning of the model anywhere. The whole system rides on a completely frozen GPT-4 Turbo or Claude 3 Opus.
[O] So the pitch is: same brain, better hands.
[G] That's a fair one-line summary. And they test it against two baselines to isolate the effect: a non-interactive retrieval-augmented setup, where a BM25 retriever pulls files and the model writes one patch in a single shot with no back-and-forth, and a shell-only agent, which is interactive but limited to the raw, unaugmented Linux shell.
[S] Let's get to numbers. What does the full system actually resolve?
[G] With GPT-4 Turbo, S-W-E-agent resolves twelve point four seven percent of the full S-W-E-bench test set, two hundred eighty-six out of two thousand two hundred ninety-four instances, and eighteen percent of the curated three-hundred-instance Lite split. The retrieval baseline gets one point three one percent on the full set. The shell-only agent gets eleven percent on Lite. Swap in Claude 3 Opus with zero interface changes, and it still resolves ten point four six percent of the full set, thirteen percent on Lite, so the design transfers to a second model, just not quite as strongly.
[O] And there's a second benchmark, HumanEvalFix, where the number is even more striking.
[G] Eighty-seven point seven percent pass-at-one on Python, eighty-nine point seven on JavaScript, eighty-seven point nine on Java, against GPT-4 on its own, which the paper reports around forty-seven to fifty percent across those same languages. That's a short-form debugging benchmark, not full repository work, so it's a cleaner test of raw editing competence, and the gap there is enormous.
[S] Now give me the part that actually explains the gain, the ablations.
[G] This is the best part of the paper. On S-W-E-bench Lite with GPT-4 Turbo, the full interface scores eighteen percent. Strip the editor down to a bare edit command with no linter: fifteen percent, down three points. Remove the special editor entirely and force redirection or sed-style edits: ten point three percent, down seven point seven points, the single biggest ablation drop in the paper. Shrink the file viewer window to thirty lines: fourteen point three percent. Show the entire file at once instead of a scrolling window: twelve point seven percent. Both directions hurt, too little context and too much context are each worse than the tuned hundred-line window.
[O] And then there's the search result, which I think is the most quotable finding in the whole paper.
[G] It is. They tried an "iterative" search interface, directly modeled on how a human uses Vim or VSCode, one match at a time, press next to see the next one. That scored twelve percent. Giving the agent no search tools at all, just cd and ls and cat, scored fifteen point seven percent. Iterative search was worse than no search.
[S] Why on earth would that happen?
[G] Because the agent doesn't behave like a human skimming results. It compulsively calls next through every single match until it exhausts its turn budget or its cost budget, and then has nothing left to actually fix the bug. The summarized search interface that shows everything at once, and tells the agent to write a narrower query if there's too much, is the one that reaches the full eighteen percent.
[O] That's the line I'd put on a slide: don't build an agent a human interface and assume it transfers.
[G] There's a behavioral thread underneath this too. Fifty-one point seven percent of GPT-4 Turbo's trajectories contain at least one failed, linter-rejected edit. Any single edit attempt has about a ninety point five percent chance of eventually succeeding, but that drops to fifty-seven point two percent right after a first failure, so error spirals are a measured, real phenomenon here, not a hunch. Successful runs are also cheap and fast, a median of one dollar twenty-one cents and twelve steps, against a mean of two dollars fifty-two cents and twenty-one steps for failures. Ninety-three percent of resolved runs finish before hitting their budget at all, which is the authors' basis for saying that just handing the agent more money or more turns probably won't help much.
[S] And when it does fail, what actually goes wrong?
[G] They had GPT-4o auto-label the unresolved instances into failure categories, checked against human labels with eighty-seven percent agreement. About fifty-two percent of failures are the agent writing an incorrect or overly narrow implementation, meaning it found the right lines and edited them, but the fix itself was wrong. Another twenty-three percent never recover from a broken edit. So even a strong interface mostly buys you accurate navigation and clean edits, not correct reasoning about what the fix should be.
[O] Okay, time for the actual argument. My case: this is a genuinely important result because it's the first paper to isolate interface design as its own lever, hold the model completely fixed, and still get a large, well-ablated swing in outcome from nothing but commands and feedback format. The iterative-search-is-worse-than-no-search finding is one of the more surprising, cleanly reproducible results I've seen in agent research.
[S] My case: the headline six point seven times number, from one point three one percent to twelve point four seven percent, is doing marketing work it hasn't earned, because the retrieval baseline it's beating isn't even interactive, no execution feedback, one shot, done. That's not a fair fight between two agents, it's a fight between an agent and a non-agent. The number I trust is eleven to eighteen percent, shell-only versus S-W-E-agent, and that's a real but noticeably smaller story.
[G] You're both right, and the paper basically hands you this distinction if you read closely. The eleven-to-eighteen comparison is the load-bearing one, same interactive setting, same model, only the interface differs, and it holds up under scrutiny. The six point seven times figure conflates "being agentic at all" with "having a well-designed interface," so Sam's skepticism there is well-founded.
[S] Thank you. Second point: every ablation in this paper, the entire design-principles argument, is run on GPT-4 Turbo only. Portability gets tested on exactly one other model, Claude 3 Opus, and Claude actually does worse with the identical interface, ten point five versus twelve point five percent. So how much of this A-C-I is genuinely general, versus tuned by hand to one model's specific quirks? The paper doesn't really settle that.
[G] Fair, and I'd call it under-discussed. The tuning process, manual trajectory inspection plus a grid search, was explicitly run against GPT-4 Turbo's behavior on a development split. We have exactly one transfer data point, and it moved in the wrong direction relative to GPT-4. I wouldn't call the interface proven model-general. I'd call it proven effective for GPT-4 Turbo, with one encouraging but incomplete transfer result.
[O] Counter-point: even a partly co-adapted interface is still real evidence that interface design matters at all, which almost nobody had measured this cleanly before.
[S] Agreed, the direction of the claim survives. It's the size and generality I'd hold more loosely. One more thing worth saying on air: the three-hundred-instance ablation table carries no confidence intervals, and the paper's own pass-at-k figure shows results swinging from roughly fifteen to roughly thirty-five percent across six runs on the identical setup. Some of those two-to-three-point ablation gaps could plausibly just be noise.
[G] The authors are candid about that variance, they show the pass-at-k curve themselves rather than hiding it. But you're right that the ablation table doesn't propagate that uncertainty into error bars, so a three-point gap and a one-point gap read as more different than they might actually be. There's also a contamination question worth flagging: S-W-E-bench is built from public GitHub issues with public fixes, and both models' training data plausibly overlaps with some of them. The authors do check that resolve rate is uncorrelated with issue age, as an indirect proxy, but that doesn't rule out leakage on any specific instance.
[O] Stepping back, what actually changes if this holds up?
[G] Two things, I think. First, "agent-computer interface" became a real design surface. Nearly every serious coding agent that followed, including contemporaneous systems like AutoCodeRover and Agentless, inherited some version of this vocabulary: don't just wire a model to a shell, design what it sees and what it's allowed to do. Second, there's a quieter lesson for evaluation work specifically. A benchmark score is really a property of the model-plus-scaffold pair, not the model alone.
[S] Which is exactly the methodology point I'd underline for this audience. If interface choices alone move the needle by seven points, then a leaderboard entry that discloses only its base model, and not its scaffold, isn't giving you a comparable number.
[G] The paper doesn't frame it in exactly those terms, but that's a defensible reading of what the ablation table implies, and it's become common practice since.
[G] If I leave you with one sentence, it's this: with the model completely frozen, redesigning nothing but the commands and the feedback took a bare-shell agent from eleven percent to eighteen percent on S-W-E-bench Lite, and the iterative-search-is-worse-than-no-search result alone is worth the read.
[O] Mine: the interface is a real, measurable lever, and "human interfaces aren't agent interfaces" is going to keep being true well past this one system.
[S] Mine: trust the shell-only-to-S-W-E-agent comparison, be skeptical of the six point seven times headline, and don't skip the failure-mode breakdown, a better interface got these agents to the right lines of code and still left about half of them writing the wrong fix. Full writeup's on the litsearch site.
