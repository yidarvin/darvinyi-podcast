---
slug: wu-2023-hps-v2
title: "Human Preference Score v2: A Solid Benchmark for Evaluating Human Preferences of Text-to-Image Synthesis"
description: "HPS v2 built a 798,090-comparison human preference dataset across nine text-to-image generators — five deliberately held out of training — and a CLIP model that predicts an individual's preference better than another individual does; its own agreement figure, plotted on a scale one hundred times off from every table in the paper, quietly bounds how finely the resulting leaderboard can be read."
date: 2026-08-03
guest_name: "Beatrix"
guest_voice: "bf_isabella"
---
[O] This model predicts which of two generated images you will prefer more accurately than a randomly chosen other human being does. That is the result, and I do not think the field has fully absorbed it.
[S] It is a real result. And in the same paper there is a figure showing that the leaderboard built on that model cannot reliably separate two models sitting next to each other on it.
[O] Both of those are true and they are not in tension. The score is excellent at the coarse question and honest about the fine one.
[S] The figure that tells you it is honest is plotted on a completely different numerical scale from every table in the same paper, and nothing anywhere flags the difference. So honest is doing some work in that sentence.
[O] Welcome to Litsearch Audio. Today's paper is Human Preference Score version two: A Solid Benchmark for Evaluating Human Preferences of Text-to-Image Synthesis, by Xiaoshi Wu, Yiming Hao and colleagues, out of the Multimedia Laboratory at the Chinese University of Hong Kong and SenseTime Research, posted in 2023.
[S] Roughly eight hundred and seventy five citations now, which for a text-to-image evaluation paper is a great deal. It became the default preference metric in that corner of the field very quickly.
[O] And joining us is Beatrix, who has spent real time inside this one. Beatrix, welcome.
[G] Thank you. And I would like to plant a flag at the very start, because it shapes everything we say afterwards. This paper is unusually well designed on the axis that matters most for a preference model, and it also contains two internal inconsistencies that a careful reader will trip over. Both of those things are true at once, and neither cancels the other.
[S] Set the scene first. Why did anyone need this in 2023?
[G] Because there was broad agreement by then that Inception Score, Fréchet Inception Distance and CLIP Score do not track what a person actually thinks of a generated image. So a first generation of learned preference models appeared to fill the gap — HPS version one, from these same authors, plus ImageReward and Pick-a-Pic. You fine-tune a vision-language model directly on human pairwise choices and use the result as a proxy judge.
[O] And this paper's argument is not that those were wrong.
[G] No, and that is the interesting move. The argument is that they carry two specific and fixable biases. The first is image-source bias. All three were built almost entirely from Stable Diffusion outputs and its variants. So a model tuned and tested on that data has no evidence at all that it generalises to a GAN, or to an autoregressive model, or to anything with different failure modes.
[S] Which is a training-distribution complaint, and a fair one.
[G] The second is prompt bias, and I find it the sharper of the two. A large share of the earlier training prompts come from Discord channels or from DiffusionDB, where users pile on style tokens. This paper counts roughly twenty eight and a half percent of DiffusionDB prompts containing the word artstation, and fifteen percent containing the name Greg Rutkowski.
[O] And those tokens only do anything if the generator was trained on data that used them.
[G] Exactly. So scoring different generator families on the same style-word-heavy prompts is not a level test. It is partly a test of whether you were trained on Artstation. And the paper adds a second complaint about the same prompts: they are frequently self-contradictory. Their example has renaissance oil painting sitting next to realism and hyper realistic in a single string, which they argue confuses the annotator judging image-text alignment as much as it confuses the generator.
[S] What about the evaluation prompt set itself? Was there a standard one?
[G] There was not, and that is the third gap. Imagen's DrawBench has two hundred prompts, which this paper says only supports the purpose of evaluation, not data collection. Too few for a stable per-model score. They keep DrawBench in their own results table purely for reference, and its reported standard deviation is larger than any of their four style sets for every one of the twenty models they benchmark.
[O] Right. So what do they actually build?
[G] Two artifacts, and it is worth keeping them apart because people conflate them constantly. Human Preference Dataset version two is the data: seven hundred ninety eight thousand and ninety binary pairwise preference choices, over four hundred thirty three thousand seven hundred and sixty images, generated from one hundred and seven thousand five hundred and fifteen prompts. Human Preference Score version two is the model — an OpenCLIP Vision Transformer H fourteen, pre-trained on LAION two billion, fine-tuned on that dataset.
[S] Take the prompt fix first. They used ChatGPT to clean the prompts, which sounds like a place to introduce a new bias rather than remove one.
[G] It is, and the paper concedes exactly that in its limitations. But look at what it does. Every prompt in the dataset comes from one of two sources: COCO Captions, used as-is, or a DiffusionDB prompt run through ChatGPT to strip the style words and the contradictions and rewrite it as one plain sentence. Their worked example takes a prompt reading queen goddess isis, high fantasy, by john berkey, by peter mohrbacher, renaissance oil painting, realism, master study, hyper-realist, and returns: the image depicts a regal depiction of the Egyptian goddess Isis.
[O] That is a dramatic reduction in surface area.
[G] And they measure it rather than assert it. One of their figures plots the frequency of prompts containing artstation, greg rutkowski and mucha, and of prompts ChatGPT itself flags as internally conflicting, before and after cleaning. The largest single drop is artstation, from roughly twenty eight percent of raw prompts to under five percent.
[S] Fine. Now the image side, which I gather is the part you think is well designed.
[G] It is the part I think is well designed, yes. The dataset draws images from nine generators plus real COCO photographs: CogView2, DALL-E 2, GLIDE, Stable Diffusion one point four, Stable Diffusion two point oh, LAFITE, VQGAN plus CLIP, VQ-Diffusion and FuseDream. Autoregressive, diffusion and GAN architectures, ranging from about point three five billion parameters up to twenty four billion.
[O] Nine families rather than one. That is already the fix.
[G] It is more than that, and this is the detail I would most want a listener to carry away. The training split uses only four of those models plus COCO images. The test split uses all nine plus COCO. Five generators — GLIDE, LAFITE, VQGAN plus CLIP, VQ-Diffusion and FuseDream — appear only at test time. They exist specifically to measure whether the trained preference model generalises to generators it never saw a single label from.
[S] I will give them that. That is the right experiment, and it is the experiment nobody in this literature had actually run.
[O] Note that the skeptic conceded inside two minutes, which I believe is a show record.
[S] My complaints are elsewhere and you will hear them. Beatrix, what about the annotation? A preference dataset is only as good as whoever clicked.
[G] Fifty seven contract annotators — fifty labelers and seven quality-control checkers. Labelers go through an orientation, then a qualifying test that involves ranking three hundred groups of images. The overall hiring rate was sixty one percent. And at least ten percent of each labeler's ongoing work is cross-checked against a checker's own ranking, scored as recall at two on the top choices.
[S] Recall at two, not exact agreement. That is a fairly loose bar.
[G] It is a loose bar, and the paper is upfront that these are ordinary annotators rather than trained art critics. But the split structure matters more here than the bar does. The test split is four hundred prompt groups, each labeled by ten distinct annotators, which is one hundred fifty three thousand binary comparisons. The training split is one hundred and seven thousand five hundred and fifteen groups with one annotator each, which is six hundred forty five thousand and ninety comparisons.
[O] And those two add to the headline number.
[G] They add exactly to seven hundred ninety eight thousand and ninety. Which brings me to the first of the two internal inconsistencies, and it is small but genuinely odd. The abstract gives that number. Section three gives that number. The arithmetic in section three point four gives that number. And then appendix H point two, the datasheet, says seven hundred eighty nine thousand and ninety.
[S] A transposition.
[G] Almost certainly a transposition, and the datasheet contradicts itself two answers later by quoting the same one hundred fifty three thousand and six hundred forty five thousand and ninety splits, which add to the other total. Nothing downstream depends on it. But a reader who goes to the datasheet section specifically to cite the dataset size — which is precisely what a datasheet is for — gets it wrong.
[O] Let's do the model. How is it actually trained?
[G] This is the elegant part, and it has consequences much later in the conversation. They do not bolt a reward head onto CLIP. They fine-tune CLIP's own cosine similarity between a prompt and an image. For a pair of images carrying a human preference label, the two similarity scores are turned into a softmax over the pair, and the model minimises the KL divergence between that softmax and the human label.
[S] So a Bradley-Terry style pairwise preference loss, sitting directly on top of CLIP's native scoring function.
[G] That is the right way to describe it. They unfreeze the last twenty layers of the image encoder and the last eleven layers of the text encoder, and train for four thousand steps with AdamW — learning rate three point three times ten to the minus six, weight decay point three five, batch size one hundred and twenty eight, five hundred warm-up steps, cosine schedule. The layer counts and the learning rate came out of Bayesian optimisation.
[O] Optimised against what target?
[G] Against accuracy on their own test set.
[O] Skeptic, do you want that one, or shall I?
[S] I will take it, thank you. Tuning your hyperparameters against the number you then report is not a hanging offence at this scale, but it does mean the headline eighty three point three we are about to discuss is not a clean held-out figure. It is a figure the architecture search was pointed at.
[G] That is a fair reading of what the paper says it did.
[O] And the last piece — how does a cosine similarity become a leaderboard number?
[G] Equation four, and it could not be simpler. They take that same cosine similarity and multiply it by one hundred. That is the whole transformation. Every HPS version two score you have ever seen quoted, every number in their benchmark table, is a CLIP cosine similarity times one hundred, which is why they land mostly in the low to high twenties rather than between zero and one.
[S] Hold that thought, because I suspect it comes back.
[G] It comes back.
[O] Table six. How does it do against the prior generation?
[G] Six models on two held-out test sets. The first is ImageReward's own test set, built entirely from Stable Diffusion images. The second is this paper's own test set, the nine generators plus COCO. Zero-shot CLIP gets fifty seven point one and sixty five point one. The Aesthetic Score Predictor gets fifty seven point four and seventy six point eight. HPS version one gets sixty one point two and seventy seven point six. PickScore gets sixty two point nine and seventy nine point eight. ImageReward's own model gets sixty five point one and seventy four. And HPS version two gets sixty five point seven and eighty three point three. Best of the six on both.
[O] And the human baselines are where it gets genuinely interesting.
[G] They report two of them. A single human predicting another single human's choice scores sixty five point three and seventy eight point one. HPS version two beats that on both columns. But a single human predicting the averaged ranking of the whole group scores eighty five on this paper's test set, and the model does not reach it.
[S] Explain why it beats one human but not the average, because I think that gap is the whole story of the table.
[G] Because it was trained on the average. Preference is genuinely diverse — you and I disagree about images and neither of us is wrong. A model fitted to many annotators in aggregate learns the central tendency, and the central tendency predicts an arbitrary individual better than another arbitrary individual does. But that individual, scored against the group average, is being measured on exactly the quantity the model is approximating, so they keep the edge.
[O] I want to defend that result harder than you just did. A model trained purely on binary clicks landing above single-human agreement is not a small thing. That was widely assumed to be the ceiling for this kind of proxy.
[S] Agreed, on their test set. Now do the other column, because that is where I have a problem. On their own test set, HPS version two beats the next best model, PickScore, by three and a half points. Eighty three point three against seventy nine point eight. On ImageReward's external test set, it beats ImageReward's own model by point six. Sixty five point seven against sixty five point one.
[O] That is a much thinner margin.
[S] It is a rounding error with a claim attached. And the claim is that this generalises better than previous metrics. The evidence for it is overwhelming on the benchmark this model was purpose-built to match, and nearly absent on the one benchmark that came from somewhere else.
[G] I think that is the strongest single criticism available, and it is visible in the paper's own headline table. I would add one nuance in the authors' favour. Their test set genuinely does contain five generator families the model never trained on, so it is not purely home turf on the image side. But the prompts are home turf, and we should come to that properly.
[O] Let's do the benchmark first. Table seven.
[G] Twenty recent text-to-image models, scored on four eight-hundred-prompt style sets — Animation, Concept-art, Painting and Photo. Each set of eight hundred is split into ten groups of eighty, so every cell is a mean plus or minus a standard deviation across those ten groups. Three thousand two hundred prompts per model in total, with DrawBench alongside for reference only.
[S] And the finding everyone quotes from it.
[G] That community fine-tunes of Stable Diffusion sit at the top. Dreamlike Photoreal two point oh at twenty eight point two four on Animation, Realistic Vision at twenty eight point two two, Deliberate at twenty eight point one three — all above both official Stable Diffusion releases, which land between twenty seven point two six and twenty seven point four eight. The paper reads this as popular models from the community consistently out-performing models from the academic side.
[O] That is a genuinely surprising sociological result if it holds.
[G] It is, though I would flag that the paper does not label each row academic, community or industry itself. You have to already know the models. And their qualitative figure, which prints scores underneath example renders, is drawn almost entirely from the community side — seven of eight renders are community Stable Diffusion checkpoints. Realistic Vision tops both prompts at thirty point eight three and thirty point five two, with ChilloutMix and Epic Diffusion trailing at twenty nine point three nine and twenty nine point two five. So it illustrates the score's granularity, not the academic versus community gap itself.
[S] What about where the model fails? There is usually a table.
[G] There is, in appendix E. They decompose pairwise agreement between the model's choice and the human choice across all forty five pairwise combinations of the nine test generators and real photographs. For every pair but two it runs between sixty seven point five and ninety eight point eight percent, which is strong. VQGAN plus CLIP is the consistent outlier — fifty one point five percent against both FuseDream and against VQ-Diffusion. Barely above chance.
[O] Three GAN-ish systems the score cannot rank against each other.
[G] Two of which it never trained on, which is precisely the generalisation case the held-out design was built to expose. To the authors' credit they report it plainly rather than bury it.
[S] I want to make my deflationary case properly now, and it is not about the generators at all.
[O] Go ahead.
[S] The images vary enormously across this pipeline. Four generators plus COCO in training, nine plus COCO at test time, twenty models on the benchmark. The prompts never vary. Every prompt anywhere in the pipeline — training, test, and the benchmark that ranks those twenty models — is either a COCO caption or a DiffusionDB prompt through the same ChatGPT cleaning instruction. They solved the generator half of their own bias critique and left the prompt half only half-solved. They cleaned DiffusionDB's tokens. They did not diversify its topics.
[O] That is fair, and I believe the paper says so itself.
[G] It does, almost in those words. The limitations section names logo and graphic design as overlooked topics which, quoting them, play a crucial role in design industries and have unique criteria. So a scoring model fine-tuned exclusively on this prompt population has no evidence its ranking transfers to a materially different one. That is not a hidden flaw, it is a declared one. But it is declared in section six and forgotten by everyone who cites the leaderboard.
[O] Alright. That is a scope limitation rather than an error. Give me the thing that actually worries you, Beatrix, because I can hear that there is one.
[G] Figure five. And I want to walk through it slowly, because the mistake involved is very easy to make and it lives in the paper, not in the reader.
[S] Set it up.
[G] Figure five plots inter-annotator agreement between two models' images against the absolute gap in HPS version two score between those two models. The finding is intuitive and important: when two models are close in score, humans disagree about which is better. When two models are far apart, humans agree.
[O] Which is exactly what you would want a sanity check to show.
[G] It is. The problem is the x-axis. It runs from zero to point zero four.
[S] Point zero four. But every score in the benchmark table is in the twenties.
[G] Correct. The figure's x-axis is on the raw, pre-rescale cosine similarity scale — the zero to one quantity, before equation four multiplies by one hundred. Every other score in the paper, and every score anyone has ever quoted from it, is the multiplied version. And nothing in the paper flags the difference. No note in the caption, no change of axis label, nothing.
[O] So if a reader takes an agreement threshold off that figure and compares it against the gap between two rows of the benchmark table.
[S] They are wrong by two orders of magnitude, in whichever direction is most flattering to whatever they already believed.
[G] Precisely. And I want to give you the corroboration, because this is not a claim you should accept on a squint. The y-axis of figure five bottoms out at sixty one point five and tops out at ninety three point one. The paper's inter-annotator agreement table, across its forty five model pairs, has an exact minimum of sixty one point six percent and an exact maximum of ninety three point one percent. Those endpoints pin the figure's points to that table's cells. It is the same data, plotted with an un-rescaled x-axis.
[O] Okay. That is not a squint, that is a fingerprint.
[S] So do the conversion for me. What does figure five actually say in benchmark units?
[G] Convert it and the trend is unambiguous. Agreement clusters around sixty to eighty percent when the score gap is below roughly one and a half points on the benchmark scale — that is point zero one five on the raw axis. It only climbs reliably past eighty five to ninety percent once the gap exceeds about two points.
[S] And how many comparisons in that twenty-model table fall under one and a half points?
[G] A great many of them. Take a concrete pair. Epic Diffusion at twenty seven point five seven on Animation, and Openjourney at twenty seven point eight five. That is a gap of point two eight, which is point zero zero two eight on the figure's raw axis. Deep inside the region where the figure shows agreement sagging toward sixty percent.
[O] So the leaderboard's finest distinctions are drawn from its noisiest ground truth.
[G] That is the sentence, yes. And it is the paper's own figure that establishes it.
[S] What about the error bars? They print a plus or minus on every cell. Plus or minus point one six three, plus or minus point one four five. Does that not cover it?
[G] No, and this is the subtle part. Those figures measure dispersion across the ten prompt subsets of eighty. That is a real and useful stability signal — it tells you the score is not an artifact of which eighty prompts you happened to draw. But it is not annotator disagreement. It is not the same quantity at all. And figure five shows the two can diverge exactly where a reader is most likely to compare two close scores and declare a winner. A gap of point two eight looks decisive against an error bar of point one six three. It is not decisive against sixty percent annotator agreement.
[O] Let me steelman properly now, because I still think this is a good paper and I want the points on the board.
[S] Please.
[O] First, the held-out generator design is the right experiment and nobody else in this literature ran it. Second, they report an external test set at all, which the prior generation largely did not. Third, the failure mode we have just spent five minutes on is documented in the paper's own figure. They did the analysis, they published it, and no previous preference paper had. The scale mismatch is a presentation bug, not a data problem. The underlying data is right.
[S] I will accept all three, and my position is narrower than it may have sounded. I am not saying the score is bad. I am saying the score has a resolution, the paper contains the information you need in order to know what that resolution is, and it presents that information in units that make it almost impossible to use. That is a real cost once the artifact becomes a leaderboard.
[G] Let me adjudicate, since that is what I am here for. On the design of the dataset, the optimist wins clearly — four models plus COCO in training against nine plus COCO at test is the correct experiment, and the paper deserves the credit it gets for it. On the generalisation claim, the skeptic wins — a point six margin on the single external test set is not enough to carry generalises better than previous metrics on its own, and the three and a half point margin comes from the home benchmark. On the resolution question I would score it to the skeptic on substance and to the authors on honesty. The analysis is there. The units are wrong. The reader is left to catch it.
[O] What changes downstream if this holds?
[G] Practically, HPS version two became a default preference metric for text-to-image work very quickly, and it gets used in two quite different modes. As a coarse comparator — is this family better than that family — it is well supported, and the appendix agreement numbers back that up. As a fine-grained tiebreaker between two similar checkpoints, it is being asked to do something the paper's own figure says the ground truth cannot support.
[S] And the general lesson is not about images at all. Any learned preference model has a resolution floor set by annotator agreement, and almost nobody reports it. The dispersion statistic that does get reported is a different quantity, and it is systematically smaller, so it reads as more precision than actually exists. That applies to reward models for language just as directly.
[O] I would add the complementarity point. This is the human-preference axis of text-to-image evaluation. A detector-based approach like GenEval asks a different question entirely — did the image contain the two objects in the right spatial relation. Neither subsumes the other, and a serious evaluation wants both, because a model can score well on preference by being pretty and badly on composition by being wrong.
[G] That is right, and the paper gestures at it. They plot their score against CLIP Score and against Aesthetic Score, and CogView2 falls out as the interesting case — high aesthetic quality, low CLIP Score. The paper reads that as good-looking images with worse controllability. Which is exactly the axis a preference score on its own cannot separate.
[S] Which is an argument for reporting a preference score next to a compositional one, not instead of it.
[G] My one-sentence version of the paper: it is the first preference model for text-to-image generation whose evaluation genuinely tests generalisation across generator families rather than within one, and its own agreement analysis quietly bounds how finely the resulting leaderboard can be read.
[O] Mine: a model trained on binary clicks that predicts an individual's preference better than another individual does is a real capability result, and the dataset design that got it there — five generator families held out on purpose — should be the template for every preference dataset that follows it.
[S] Mine: read the units on every axis, check that a paper's arithmetic agrees with its own datasheet, and never let a small gap on a leaderboard mean more than the humans who built it could agree on.
[O] The full writeup, with the figures, the tables and the numbers we quoted, is on the litsearch site under Human Preference Score version two. Beatrix, thank you.
