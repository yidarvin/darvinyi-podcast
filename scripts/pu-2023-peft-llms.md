---
slug: pu-2023-peft-llms
title: "Empirical Analysis of the Strengths and Weaknesses of PEFT Techniques for LLMs"
description: "Three researchers dig into a controlled Scale AI benchmark where full fine-tuning out-converges every parameter-efficient method at low data, then argue over whether the method rankings survive a second random seed."
date: 2026-07-19
guest_name: "Maya"
guest_voice: "af_bella"
---
[O] Here's a sentence that should stop you mid-scroll: touching less than one percent of a model's weights can take you six times longer to train than just updating all of them.
[S] And it's not a hypothetical, this paper watched it happen in their own runs. The entire pitch of parameter-efficient fine-tuning was "efficient," full stop, and here it's the slow option.
[O] Except quality doesn't collapse. Some of these tiny-parameter methods actually beat full tuning on accuracy at low data. So which is it, efficient or not?
[G] Both, depending which axis you're asking about. That tension, efficient in parameters but not necessarily in time or in performance, is exactly what this paper sets out to map.
[O] Welcome to Litsearch Audio, where an optimist, a skeptic, and a visiting scholar take apart one paper from litsearch dot darvinyi dot com. I'm your optimist.
[S] And I'm the skeptic, here to make sure the hype survives contact with the appendix.
[O] Today's paper is "Empirical Analysis of the Strengths and Weaknesses of PEFT Techniques for LLMs," by George Pu, Anirudh Jain, Jihan Yin, and Russell Kaplan at Scale AI, published at the ICLR twenty twenty-three workshop on Understanding Foundation Models.
[S] Joining us is Maya, who's spent a lot of time inside this paper's tables. Welcome, Maya.
[G] Thanks for having me, happy to be here.
[O] So why does this one belong on the docket, Maya? By twenty twenty-three, parameter-efficient fine-tuning had basically become the default way to adapt a large language model, but nobody had run the actual head-to-head.
[G] Right, every technique, LoRA, IA-cubed, prompt tuning, BitFit, had been evaluated in its own silo, on different models and different datasets. This is the first paper to hold the model, the splits, and the metrics fixed across all of them at once.
[S] Walk us through the gap. Why did the field need this and not just another method paper?
[G] Because by twenty twenty-three you had at least five competing PEFT recipes, each claiming an edge, but each benchmarked against a different backbone. A practitioner had no principled way to answer two very practical questions: which method for my task and data budget, and does the promised efficiency even survive contact with real training?
[O] And the actual motivation for PEFT is a serving problem, isn't it, not really a training problem.
[G] Exactly, the paper's own framing is about scale. Once a model has hundreds of billions of parameters, hosting a full separate copy of the weights per task is cost-prohibitive, and reloading all the weights every time you switch tasks is too slow. PEFT's promise is you freeze the backbone and hot-swap a tiny module, so one frozen model serves many adapted tasks.
[S] So the pitch is architectural and economic, not "trains faster."
[G] Correct, and that distinction matters later, because the paper ends up measuring training wall-clock, which isn't actually the thing PEFT was built to optimize.
[O] Hold that thought, we'll come back to it. What did the prior landscape actually look like?
[G] Scattered. Adapter layers from Houlsby and colleagues, prompt tuning from Lester and colleagues, LoRA from Hu and colleagues, IA-cubed from Liu and colleagues, BitFit from Zaken and colleagues, each its own paper, its own backbone, its own claim of superiority. There was a smaller RoBERTa-scale study finding PEFT does better with less data, but nothing at language-model scale, and nothing crossing task types.
[S] So the contribution here is discipline, not novelty. No new method.
[G] None. This is entirely an empirical audit: one model, four methods, a full-tuning baseline, three data scales, two task types.
[O] Let's get into it, then. What's the one model everything runs on?
[G] FLAN-T5-XL, an instruction-tuned encoder-decoder with two point eight five billion parameters. It wasn't an arbitrary pick, either. During an exploration phase the authors tried GPT-Neo, BLOOM, and other T5 variants and found, in their words, noticeably worse performance across the board, so they fixed FLAN-T5 as the representative large language model.
[S] I want to flag that early, because "noticeably worse" is a decision the authors made, not a number they showed us.
[G] Fair, we'll come back to that. Against FLAN-T5 they run four PEFT methods, each intervening at a different level of the architecture, plus a fully fine-tuned baseline trained on identical splits.
[O] Give us the four, Maya.
[G] LoRA sits at the parameter level, a low-rank update to the attention weights, here at rank two, which is quite low, contributing about three and a half million trainable parameters. IA-cubed, written eye, A, in parentheses, cubed, not to be confused with A-I, also works at the parameter level, learning element-wise rescaling vectors on the key and value attention and the feed-forward activations. That name comes from the original IA-cubed paper, not this one, and stands roughly for inhibiting and amplifying inner activations. It's the cheapest of the four, under a million parameters, and uniquely, the scaling folds directly into the existing weights, so there's nothing extra to store at serving time.
[S] That last point matters a lot for the actual PEFT use case, hosting many adapters cheaply.
[G] It does, and it shows up in the paper's own recommendations later. Third, prompt tuning works at the feature level, one hundred trainable soft prompt tokens prepended to the input embeddings, around two hundred thousand parameters. And fourth, BitFit works at the model level, you fine-tune only the bias terms of the linear layers, about one point two million parameters. Every one of these touches under one percent of FLAN-T5's weights.
[O] And the tasks?
[G] Two classification datasets, AG News, four classes, over one hundred thousand examples, and CoLA, a two-class linguistic acceptability task with around ten thousand examples. Two generation datasets, E2E NLG, restaurant descriptions, fifty thousand examples, and SAMSum, abstractive dialogue summarization, around fifteen thousand examples. Classification is scored by exact-match accuracy, generation by ROUGE-L.
[S] And the data-scale axis is the actual experimental design here.
[G] Right, that's the whole point. Every dataset runs at three scales, low-resource at most one hundred examples, medium at most one thousand, high at most ten thousand, with validation and test capped at twenty-five hundred. Training used four A10 GPUs with model parallelism, HuggingFace Transformers plus DeepSpeed, epoch caps of ten, five, and one for low, medium, and high resource, and early stopping whenever validation loss stopped improving.
[O] Ten epochs at low-resource but only one at high-resource, that's a big asymmetry.
[G] It is, and it's a compute-budget choice, not a neutral one, worth flagging because it feeds directly into the convergence story. There's also a second prong, an ablation on the two most configurable methods, LoRA and IA-cubed. Instead of applying the adapter everywhere, the authors vary which layers it touches, early, later, or a random half, and which sub-modules, dropping self-attention, encoder-decoder attention, the query and output vectors, or the dense activations, to see how far you can strip it down before performance breaks.
[S] So two separate questions: which method wins, and how cheap can you make the winner.
[G] Exactly those two.
[O] Let's get to the headline number, because it's the one that surprised me.
[G] In low- and medium-resource settings, full fine-tuning converged faster than every single PEFT method, with one exception, BitFit on CoLA at medium-resource. At low-resource, full tuning's speed advantage was seventy-three percent on AG News, eighty-seven percent on CoLA, sixty-six percent on E2E NLG, and sixty percent on SAMSum. At medium-resource it narrows but doesn't disappear, forty-six, sixteen, thirty-seven, and sixty-four percent respectively.
[S] So parameter-efficient does not mean compute-efficient. That's worth sitting with, the entire pitch of these methods was "cheap," and here they're the expensive option to actually train.
[G] That's the authors' own framing, and their explanation is about overfitting. At tiny data sizes, full tuning quickly learns and overfits the small dataset, so it hits early-stopping fast. PEFT methods, they argue, learn unstably at that scale and need more epochs to get anywhere, but stabilize and converge faster as data grows.
[O] Which is almost a backhanded compliment to full tuning, it's not converging fast because it's good, it's converging fast because it's overfitting.
[S] Right, "fast" and "good" are doing different work in that sentence, and we should come back to that.
[G] We will. On quality, the picture is a genuine mixed bag, no single PEFT method dominates. At low-resource, the best methods actually beat full tuning: BitFit reaches about eighty-eight percent on AG News against full tuning's roughly eighty-six percent, LoRA close behind at about eighty-six percent as well. But as data grows, full tuning claws back ahead, by high-resource it's at ninety-three point four percent on AG News, ahead of everything.
[O] So there's a crossover point, low data favors PEFT on quality, more data favors full tuning.
[G] Exactly, and prompt tuning is the consistent laggard across the board, it never wins a single row, and it effectively collapses on generation. At low-resource its ROUGE-L on E2E NLG is around point zero three, and on SAMSum it's under point zero one. That's close to producing nothing useful.
[S] That's not "slightly behind," that's broken.
[G] It's broken at that data scale with these hyperparameters, yes, a hundred soft tokens on whatever schedule the original prompt-tuning paper recommended, not re-tuned for FLAN-T5. We'll get to whether that's a fair test of the method itself.
[O] What does the paper actually recommend, given all this?
[G] They fold speed and quality into a three-way decision rule. If you're time-constrained, use full tuning at low and medium resource, switch to PEFT once you're in the high-resource regime. If you're memory-constrained, BitFit and IA-cubed are your low-resource picks, IA-cubed alone at medium and high resource, because it has essentially no extra parameters to store at serving time, the rescaling folds into the existing weights. If you're performance-constrained, IA-cubed, LoRA, or BitFit at low and medium resource, full tuning once you have enough data. And if you genuinely only have a handful of examples, they suggest prompt engineering or in-context learning might just beat fine-tuning anything at all.
[O] That's a genuinely useful, concrete decision tree, I can imagine handing that to an engineer directly.
[S] I'll grant that's the paper's strongest practical contribution. Now, the ablation, that's the part I found most interesting.
[G] It's also the most actionable finding. By restricting LoRA and IA-cubed to only the later layers of the model, the authors cut trainable parameters by roughly half, IA-cubed goes from around nine hundred thirty-four thousand down to about four hundred sixty-seven thousand, LoRA from three and a half million down to about one point seven million, while maintaining, and in several cases improving, performance.
[O] Half the parameters for free, essentially.
[G] Essentially, but the two methods behave very differently under that surgery. LoRA is robust, cut wherever you like, performance barely moves. IA-cubed is fragile. Restrict it to early layers and AG News craters from around ninety-two percent down to sixty percent. Drop its attention modification entirely and SAMSum's ROUGE-L falls from around point four three to about point two four.
[S] So "you can halve the parameters" is true for LoRA unconditionally, and true for IA-cubed only if you're careful about which half.
[G] Precisely, and in both cases later layers consistently beat earlier layers, which lines up with the transfer-learning intuition that later representations carry the task-specific signal, while the earlier layers are doing more generic feature extraction.
[O] Okay, time for the part I've been waiting for. My case: this paper is a genuine public service. It killed a piece of folk wisdom, "parameter efficient" quietly became "cheap and fast" in a lot of people's heads, and now there's controlled evidence that at low data, on a real three-billion-parameter model, that's false. Plus the ablation is a free lunch, cut LoRA's footprint in half with no quality cost. That's immediately actionable for anyone serving adapters in production.
[S] I'll take the deflationary side. Start with the model, n equals one. Every claim here about "LLMs" rests on a single two point eight five billion parameter encoder-decoder, and the authors explicitly dropped GPT-Neo and BLOOM, real decoder-only alternatives, because they performed "noticeably worse," without showing us the numbers. That's not evidence PEFT behaves this way generally, it's evidence FLAN-T5 behaves this way.
[G] That's a fair read of the appendix, the exclusion is stated but not quantified. And it compounds with something else: FLAN-T5 is already instruction-tuned on a huge mixture of tasks. Classification like AG News, dialogue summarization, these are exactly the kinds of tasks that mixture likely covers. So low-data adaptation might look easy partly because the model already has the relevant structure, not because PEFT itself is naturally strong at low data.
[O] Okay, that's a real point, Maya, but does it undercut the training-speed result, or just the quality numbers?
[G] Mostly the quality numbers. The speed result is more about optimization dynamics than prior knowledge.
[S] Second issue, hyperparameters. One fixed configuration per method, reused across every scale and task, pulled from each method's original paper, tuned on a different model and dataset. LoRA's pinned at rank two, tiny. Prompt tuning's pinned at one hundred tokens. And prompt tuning is exactly the method that collapses on generation, point zero three ROUGE-L. Is that PEFT failing, or an under-tuned configuration failing?
[G] The paper doesn't disentangle that, and prompt tuning is known from other work to need scale and longer schedules before it's competitive. I'd put a caveat flag directly on that last-place finish.
[O] Fine, I'll concede prompt tuning's specific ranking is shaky. But the core finding, full tuning converges faster at low data, that one survives, doesn't it?
[S] Does it, though? Look at the actual margins. LoRA edges full tuning on low-resource AG News by about a quarter of a percentage point. And the appendix admits they only spot-checked variance across two seeds on their early exploration runs, not on the headline FLAN-T5 numbers. With gaps that small on datasets of a hundred examples, I don't trust the ordering.
[G] That's the sharpest critique in the room, and I'd score it as a real hit. Several low-resource orderings, including that AG News gap, are inside what a second random seed could plausibly flip. The bigger gaps, BitFit's two-point lead on the same task, or prompt tuning's near-zero ROUGE-L, those are robust to noise. The knife-edge comparisons aren't.
[O] Point taken. But I still think the headline framing survives even with that discount, "PEFT isn't automatically a training-speed win" is true even if some individual rankings are noisy.
[S] Agreed, that framing survives. I just don't trust the specific league table underneath it.
[G] And one more adjudication, on the speed claim itself, it's entangled with the epoch caps and the early-stopping rule the authors chose. Full tuning "wins" partly because it overfits fast and hits early stopping sooner. Whether reaching an overfit solution quickly counts as a genuine efficiency win is a judgment call, not a fact the data settles.
[O] That's a great point actually, "fast" isn't obviously "good" there.
[S] Which is exactly my objection from a few minutes ago, so, thank you.
[O] So what actually changes if you take this paper's core finding seriously, Maya?
[G] Practically, if you're picking a fine-tuning method under a training-time budget, on a model in this size class, with only a few hundred examples, full tuning deserves serious consideration, not just PEFT by default. And the ablation result stands regardless of the debate we just had, pruning LoRA to later layers is a clean, low-risk trick if you're already committed to LoRA.
[S] I'd add a methodological lesson that connects to the broader evaluation work we usually cover on this show, this paper is honest about its own limits in a way a lot of benchmark papers aren't. It names its single-seed problem, its fixed-hyperparameter problem, its model-scope problem, right in the text.
[O] That transparency is worth crediting on its own, separate from whether every conclusion holds up.
[G] Agreed, and the bigger open question the paper leaves for the field is the one it never tests, actual serving-time memory and multi-adapter hosting throughput, which was the original economic case for PEFT in the first place. Everything here measures training wall-clock, on a three-billion-parameter model, one step removed from the hundred-billion-parameter serving scenario the introduction actually motivates.
[O] So the natural next paper is the same benchmark, but measured at the point of serving, not training.
[G] That's exactly the gap I'd want filled next.
[G] My one-line takeaway: this paper's real contribution is the discipline of a fixed, controlled comparison. Read its decision rule as a lead for small-data, FLAN-T5-scale adaptation, not a settled verdict on PEFT for today's much larger models.
[O] Mine: don't assume "efficient" without asking efficient at what, parameters, training time, and serving memory are three different axes, and this paper is the first place I've seen them pulled apart this cleanly.
[S] And mine: trust the big gaps, distrust the knife-edge ones, and always ask what a second random seed would do to a headline number. That's it for this episode of Litsearch Audio, the full writeup, with every table and the ablation figures, is on the litsearch site. Thanks for joining us, Maya.
[G] Thanks for having me.
