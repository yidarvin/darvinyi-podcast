---
slug: allenzhu-2023-knowledge-manipulation
title: "Physics of Language Models: Part 3.2, Knowledge Manipulation"
description: "Zeyuan Allen-Zhu and Yuanzhi Li build a synthetic biography dataset they wrote themselves, so they can prove a model retrieves stored facts near-perfectly yet needs chain-of-thought at both training and inference to classify or compare them, and can barely search backward from an attribute to a person at all."
date: 2026-08-14
guest_name: "Freya"
guest_voice: "bf_emma"
---
[O] Here's a strange thing this paper does. It builds a language model that can tell you a made-up person's exact birthday, city, university, and employer, basically every time — we're talking close to one hundred percent.
[S] Then it asks a completely symmetric question. Fine, you know everyone's birthday. Who was born on this exact date? And accuracy falls off a cliff, down toward zero.
[O] Not "gets worse." Zero. Across three hundred twenty different combinations of pretraining setup and question phrasing, almost none of them ever crack two percent, and half of them land at exactly zero.
[S] What makes me sit up is that this isn't some messy benchmark where you can argue about what "zero" even means. The authors wrote every fact in this dataset themselves. There's no missing information to blame here.
[O] Welcome to Litsearch Audio, where we take one paper off the map at litsearch dot darvinyi dot com and actually argue about it. I'm your optimist for the hour.
[S] And I'm the skeptic. Today's paper is Physics of Language Models, Part three point two, Knowledge Manipulation, by Zeyuan Allen-Zhu at Meta AI's FAIR lab and Yuanzhi Li at the Mohamed bin Zayed University of Artificial Intelligence, in Abu Dhabi. Posted to arXiv in September of twenty twenty-three, it eventually landed at ICLR twenty twenty-five, and it's sitting at one hundred sixty-one citations and counting.
[O] And joining us to dig into it is Freya, who's spent real time with this one. Freya, welcome.
[G] Glad to be here. This is one of the more satisfying entries in the "physics of language models" series to sit down and read end to end, because the name is doing real work — it's styled like physics. Control the variables, run the controlled experiment, then go check whether the law holds up in the wild. This installment is about whether a model can actually use knowledge it stores, not just recite it.
[S] Let's set up why you'd need a purpose-built dataset for that, instead of just testing GPT-4 on some existing benchmark. What's wrong with, say, asking a model whether Aristotle would have used a laptop, or whether a pear sinks in water, and seeing if it reasons its way there?
[G] Two things confound that immediately. One, the model may have seen an equivalent sentence somewhere in pretraining, in which case you're measuring retrieval dressed up as reasoning, and you can't tell the difference. Two, even if it didn't, you have no way to prove a negative about a web-scale corpus. You can't audit what a model has and hasn't seen inside trillions of tokens.
[O] So this paper's move is almost brute-force honest. If you can't control what a production model saw, build your own model and control every sentence that goes into it.
[G] Exactly. They invent up to one hundred thousand fictitious people, each with six independent attributes — birth date, birth city, university, major, employer, and the employer's city — and they write every biography sentence themselves. Nothing is scraped, nothing is ambiguous about exposure.
[S] This actually connects to the paper we covered last time out, the sycophancy one. That paper's whole argument rested on the phrase "despite knowing the statement is wrong," and it establishes that purely from behavior — watching outputs flip under social pressure.
[G] Right, and this is a clean contrast. Here, "the model knows it" isn't inferred from behavior at all. It's measured directly, because the authors control the ground truth. They can literally check whether a given model, on a given attribute, extracts it correctly close to one hundred percent of the time. If yes, that's storage, established by construction, not implied by a downstream behavior.
[O] Which is a strictly stronger kind of evidence.
[G] In that setting, yes. Hold onto "in that setting," though, because that's exactly where the interesting fight in this paper lives, and we'll get there.
[S] Walk us through the four tasks, concretely, Freya.
[G] Retrieval is the baseline case — "what is person A's birth date." Classification takes a stored attribute and asks something like whether the birth month is even or odd. Comparison asks about two people at once — is A's major luckier than B's — either as a yes-or-no ranking question, or as an actual numeric subtraction of two attribute codes. And inverse search flips the whole direction — instead of person-to-attribute, you're given the attribute and have to recover the person. Which person was born on this date, in this city?
[O] There's a diagram early on, Figure one, that I think is doing more work than it looks like at first glance.
[G] It's the paper drawing a hard boundary around what it's actually claiming. In scope is exactly one path — the model infers the answer from knowledge it stored during pretraining. Out of scope, and explicitly labeled as such: the model having simply seen the exact question before, which is memorization dressed up as manipulation; the model not actually knowing the fact to begin with, a different failure mode covered in a companion paper in the same series, Part three point one; and the model not understanding the operator itself, not knowing what "even" even means as a concept. Rule those out, and only then is the rest of the paper interpretable.
[S] How do you rule those out mechanically?
[G] Split the hundred thousand people into a training half and a held-out half, which the paper calls P-test. Every person's full biography is in the pretraining data for both halves, so extraction ability is identical across the split by construction. Then you fine-tune, with LoRA, on manipulation question-answer pairs, but only for the training half. P-test people get evaluated on manipulation questions the model has never seen an answer for, even though it's memorized their biography cold.
[O] So a model that just memorizes training-set answer patterns would still fail on P-test.
[G] That's the whole point of the split. There's a second axis crossed with all of this — whether the fine-tuning data includes a hint. In "trained without hint," the model only ever sees question, then answer. In "trained with hint," half the examples spell out the relevant attribute before the answer, so the model can learn to generate its own reasoning step. At test time, a no-hint-trained model is only ever tested without a hint. A hint-trained model gets tested both ways.
[S] That's three of four possible combinations. Trained without, tested without. Trained with, tested with. Trained with, tested without. What's the fourth?
[G] Trained without hint, then simply prompted for chain of thought at inference time, no fine-tuning involved at all. That specific cell never appears in the controlled experiment. Hang onto that, it matters later.
[O] Before we get to whether any of this works, what does "perfectly stored" actually cash out to, numerically? That's the premise the entire rest of the paper leans on.
[G] It's a specific, checkable number, not a qualitative gesture. Most of what we're about to walk through runs on GPT-2, small enough to iterate on fast, and version two of the paper adds Llama and Mistral replications on top, specifically to check the pattern isn't a GPT-2 idiosyncrasy. The core model, pretrained on what the paper calls bioS multi5+permute, five diversely phrased, randomly shuffled biography variants per person, extracts birth dates at one hundred percent accuracy on held-out individuals, and majors at ninety-seven point seven percent. That's the anchor. Whatever failures show up next, they are not failures of storage.
[O] Let's get to the actual numbers, starting with retrieval, since that's supposed to be the easy case.
[G] Retrieval alone is basically solved, once the pretraining data is diversified enough. Ask for two attributes in one answer, say, which company someone worked for and in which city, and with the best-augmented setup you're scoring in the ninety to one hundred percent range across the board, against a baseline near zero.
[S] "Best-augmented" is doing some work in that sentence. What happens with a less diversified setup?
[G] You get an ordering effect I find genuinely interesting. Take a version of the pretraining data where the company name always appears before the company's city, in that fixed order, in every training sentence. Ask the model for company, then city, matching the order it was trained on, and you get ninety-four point nine percent. Ask about the exact same fact, but city, then company, against the order it was trained on, and accuracy collapses to seventeen point four percent.
[O] Same underlying fact, same model, just asked in the "wrong" order, and it loses most of its ability to answer.
[G] Right. And there's a second retrieval failure that's even sharper, because it doesn't need any ordering trick at all. Take the model that hits one hundred percent on "what is this person's full birth date." Ask that exact same model "what year were they born," which is just a piece of an answer it can already produce perfectly, and accuracy on that specific question falls to twenty point four percent.
[S] Wait, that's the same information. If it can produce the full date, the year is right there inside the answer it already knows how to generate.
[G] That's exactly the authors' point, and their reading is that the model has to generate the attribute in the order it was trained to produce it, month, then day, then year, before it can hand you a piece in isolation. It can't skip ahead to a token in the middle without walking through what comes before it.
[O] That's a genuinely strange finding to sit right next to a paper calling retrieval a solved problem.
[G] It's the one place the paper's own retrieval story is less than clean, and to its credit, it's presented that way, not buried in a footnote.
[S] Let's get to the part of the abstract I think is the real headline, struggling at classification and comparison without chain of thought.
[G] Take a model that's already a near-perfect extractor, same one hundred percent, ninety-seven point seven percent numbers as before. Ask it to classify whether a birth month is even or odd, with no hint anywhere in training. It needs ten thousand fine-tuning examples just to reach seventy-five point nine percent. Ask it to rank two birth months, which one comes earlier, and it needs fifty thousand examples to reach eighty-five point six percent.
[O] Those sound like a lot of examples for something this simple. There are what, twelve possible birth months?
[G] Right, and that's exactly the authors' complaint. In theory you'd expect something on the order of twelve examples, or twelve squared for the pairwise version, to nail a task over twelve categories, if the model is genuinely retrieving and then computing. Instead it needs tens of thousands, which reads as evidence that whatever it's doing isn't retrieve-then-compute, it's closer to memorizing the answer pattern from scratch.
[S] Give me the sharpest version of this.
[G] Comparing across one hundred possible majors. Each major gets a fixed numeric code, and the subtraction task asks how much luckier one person's major is than another's, an actual numeric subtraction. At fifty thousand training examples, that's one percent. Scale up fifty-fold, to two point five million examples, and it's one point two percent. Baseline is one percent. It never leaves the baseline.
[O] Two and a half million examples and it doesn't move at all.
[G] It's the flattest line in the whole paper, and it's the paper's own headline "barely random guessing" example. But here's a precision point worth making, because I checked the actual figure against the paper's own summary bullet, and they don't quite say the same thing.
[S] Go on.
[G] The main-text bullet list describes this as "ranking one hundred majors barely outperforms random." But ranking, the yes-or-no, "is A luckier than B" version of the same comparison, is not flat. It climbs from a fifty point five percent baseline to sixty-nine point seven percent at two point five million examples. That's a real, if incomplete, gain. It's specifically subtraction, the numeric version, that never moves. The figure's own caption gets this right and names subtraction specifically; it's the prose bullet elsewhere that blurs the two together as "ranking."
[O] So even inside a paper this careful, a summary sentence can be looser than the figure it's summarizing.
[S] Which is exactly why you read the figure and not just the bullet list underneath it.
[G] Now, does training with hints fix any of this? Only if you also use the hint at test time. Stay on the ten-thousand-example birth-month case. Trained without any hint, tested without one, seventy-five point nine percent, the number we already had. Train with hints half the time, then test without a hint anyway, eighty point three percent. Barely better, four points, despite the model having seen chain of thought during training.
[O] So training-time exposure to reasoning basically doesn't transfer to a model that isn't allowed to reason at test time.
[G] Correct. But test that same hint-trained model and let it actually use its hint at inference, and it jumps to ninety-four point two percent. And the hint it generates for itself is correct ninety-one percent of the time, on its own.
[S] So the ability was achievable all along, it's specifically gated on producing the intermediate step out loud, at inference, not just having trained near one.
[G] That's Result four, essentially. And Result five closes a possible loophole, maybe a model separately fine-tuned just for knowledge extraction gets better at manipulation as a side effect. It doesn't. The extraction-finetuned and manipulation-finetuned columns track each other closely, task by task, rather than one pulling ahead of the other.
[O] Does any of this show up outside the synthetic sandbox, on an actual production model?
[G] That's Result six. Take real Wikipedia biographies, about four thousand seven hundred seventy-nine celebrities, and ask GPT-4 the same style of question. Pure retrieval, birthday and year, ninety-nine percent. Classify whether the birth month is even, fifty point seven percent correct. That's a coin flip.
[S] On real people it presumably has actually memorized well.
[G] That's exactly the point, it retrieves the underlying fact close to perfectly and still can't reliably tell you whether the month is even. Ranking is more interesting, because it's not flat, it's graded. Compare two celebrities both born between nineteen hundred and nineteen ten, and you're near chance, fifty-two point three percent. Widen the window to nineteen hundred through nineteen fifty, seventy-one point one percent. Take all pairs regardless of era, eighty-one point six percent.
[O] That gradient probably tracks how much web data exists about each era, more recent, better-covered people are easier to compare.
[G] That's the natural reading, and the paper doesn't overclaim beyond showing the pattern. Result six isn't a new number so much as confirmation that the synthetic story shows up qualitatively on a real deployed model too.
[S] Let's close the loop on the number we opened with. Inverse search.
[G] Result seven. Sixteen different "ordinary" pretraining configurations, crossed with twenty different ways of phrasing the inverse question, ask for the first name only, ask for the full name, from one attribute or a combination of up to four. That's three hundred twenty cells. Every single one is at or below one point seven percent.
[O] And they don't all sit at that same one point seven percent, right? You mentioned something about how those cells split.
[G] Right, worth being precise here, because it's easy to misquote as one number. Half the columns, the ones asking for just the first name, land somewhere between zero point six and one point seven percent. The other half, the ones asking for the full name, are exactly zero percent, in all sixteen rows. Not close to zero. Zero.
[S] Zero is a strong claim for a trained neural network to hit that consistently.
[G] It is. And the only place accuracy recovers is four control rows where the authors literally rewrote the biography text so the attribute appears before the person's name in the source document, reversing the order it was written in. Those recover sharply, into the nineties, but only on the column groups matching that specific reversal direction. The paper is upfront that this isn't a counterexample, once the order is truly reversed in training, in their words, the task is no longer inverse knowledge search. It's a positive control confirming the mechanism, not a crack in the result.
[O] What's the actual explanation for why this happens at all?
[G] The paper attributes it, explicitly, to left-to-right autoregressive training. If the model learns "A implies B," it has no built-in mechanism to infer "B implies A" unless that direction was separately present in the data, period, that's the word the authors use for it. And they pre-empt the obvious counter, what about a bidirectional model, something like BERT. They cite their own companion paper's finding that BERT already struggles with plain forward extraction, so bidirectionality isn't a free fix, it trades one problem for a worse one.
[S] I want to flag something about that, because it's citing a result, not re-deriving it here.
[G] Correct, that's borrowed evidence from a sibling paper, not reproduced in this one. Worth remembering when we get to how far "inherent" is licensed to travel.
[O] Does inverse search fail on production models too, the same way retrieval and classification did?
[G] Result eight, and it's consistent across everything they tried. Give GPT-4 Jane Austen. Predict the next sentence of Pride and Prejudice, sixty-five point nine percent. Predict the sentence that came before it, same book, same model, zero point eight percent.
[S] That's a massive gap for what's structurally a very similar task.
[G] And it's not a one-book fluke. Same pattern across five more Austen novels, forward always dramatically ahead of inverse, twelve out of twelve paired comparisons, no exceptions. Wikipedia biographies, predict the person's name from their date, city, and occupation, twenty-three point nine percent for GPT-3.5, forty-two percent for GPT-4, against eighty-nine point five and ninety-nine percent forward. Two Chinese-language tasks too, idiom letter-infilling and classical poetry, both showing the identical direction.
[O] The idiom one is a nice granular version of this, isn't it, since it's not just binary forward-versus-backward?
[G] Right, they mask one letter out of a four-letter idiom, and vary which letter, from the most inverse-like position to the most forward-like. Accuracy increases monotonically as you move toward the forward-like position, for both GPT-3.5 and GPT-4, across all four positions, with no exceptions. It's the cleanest real-world confirmation of the directional story anywhere in the paper.
[S] Does the paper offer any actual way out of this?
[G] Three mitigations, Result nine, and it's worth being honest that they don't all carry equal evidence. Rewriting pretraining data to include the reversed direction is actually measured, that's the reversal rows we just discussed. Retrieval-augmented generation gets a citation to prior work; it's not tested in this paper at all. And inserting explicit line numbers into a document, illustrated with a single Bible passage where GPT-4 uses chapter and verse numbers as a counting aid, is one qualitative example, not a measured accuracy result.
[O] So one real fix, one borrowed idea, and one anecdote.
[G] That's a fair summary, and the paper doesn't dress the anecdote up as more than it is.
[O] Let me make the strongest case for this paper, because I think the methodology is genuinely excellent. Every other paper trying to test whether a model can manipulate its own knowledge runs into the same wall, you can't prove what a giant web-scraped model has or hasn't seen. This one sidesteps the whole problem by writing the pretraining data itself, so it can directly measure storage and directly measure manipulation on the same held-out individuals, with a real train-test split guarding against the model just memorizing question patterns. And it's not a one-architecture fluke, version two adds Llama and Mistral replications of the same synthetic experiments, and the pattern holds across all three.
[S] I'll grant all of that, that part is close to as clean as controlled machine learning research gets. My question is about one word in the abstract: "inherent." Freya, how far does the evidence actually let that word travel?
[G] This is the sharpest place to push, and I think the paper itself is honest about the boundary, even if the abstract reads more sweepingly. The rock-solid version of the claim, perfectly stored, yet unmanipulable, is established for controlled synthetic biographies, replicated across three architectures. The real-world evidence, GPT-4 on Wikipedia, GPT-3.5 and GPT-4 on Jane Austen and Chinese text, is real and it's consistent, every paired comparison points the same direction. But the paper says outright: "it remains unclear if GPT-4 has seen these questions during its pretraining." That's the authors themselves flagging that the real-world half isn't controlled the way the synthetic half is.
[O] So corroborated, but not controlled, on the production-model side.
[G] That's exactly the right way to put it. The strongest, most certain version of the claim is scoped to the synthetic setting. Extending it to "this is just how language models work" is a step the evidence supports qualitatively, but doesn't nail down the way the core experiment does.
[S] There's a second gap I want to raise, and it's a specific ablation the controlled experiment never runs. We mentioned earlier there's a missing fourth cell, trained without hint, then simply prompted for chain of thought at test time only, no fine-tuning at all.
[G] Right, and here's why that matters more than it might sound like. Every real-world "chain of thought fixes it" example in this paper, the ones with actual GPT-4 transcripts, is exactly that condition. A production model the authors never fine-tuned, unlocked purely by asking it to state the attribute before answering. But the controlled experiment that would tell you whether that specific mechanism, prompting alone with no training, works on the paper's own checkpoints, is the one condition Figure four doesn't include.
[O] So the qualitative demonstration and the quantitative experiment are answering two adjacent, but not identical, questions.
[G] Precisely. It doesn't break the paper's claims, but it does mean "chain of thought during both training and inference" is proven in the controlled setting, while "chain of thought at inference alone, no training" is only shown anecdotally, on a model the authors didn't build.
[S] Can I push on how much that actually matters, though? Even granting all of that, the core number, three hundred twenty cells, all at or under one point seven percent, over half of them at exactly zero, isn't a borderline result some missing ablation threatens. That's about as unambiguous as an empirical finding gets.
[O] Wait, I think you two have swapped sides on me for a second there.
[S] Fair, I'm not disputing the number itself, I'm disputing exactly how far the word "inherent" is entitled to travel from that number, out to real corpora, to models this paper didn't build or fine-tune.
[G] And that's the right line to draw. Where I'd land, adjudicating both of you: the synthetic result is essentially airtight on its own terms, and the real-world corroboration is genuinely consistent, not cherry-picked. But "inherent to language models," full stop, claims a little more certainty than the paper's own hedges support once you get to production models on real corpora. The paper is honest about that gap in its own text. The abstract just doesn't carry the hedge along with it.
[O] Let's zoom out to something practical, since a lot of people listening to this build or evaluate systems for a living. What does this actually change about how you'd build something?
[G] The clearest case is anything that looks like retrieval-augmented generation. You fetch two facts correctly, say two numbers pulled from two different documents, and you assume the model can then compare or combine them because it clearly "has" both facts in context. This paper's whole point is that having a fact and being able to operate on it are different capabilities, and the gap doesn't close itself just because retrieval worked.
[S] Which has a direct evaluation-design consequence. If you're building a benchmark meant to test reasoning, and the model can answer it by retrieving something equivalent from pretraining, you're not testing what you think you're testing. This paper's train-test split on held-out individuals, extraction uniform across the split by construction, manipulation tested only on people the model never saw an answer key for, is a genuinely reusable pattern for isolating "did it reason" from "did it remember," well beyond this one dataset.
[O] And on the optimistic side, the mitigations, even the underdeveloped ones, point at concrete levers. Reverse-augmented pretraining data, actually measured here, and something as cheap as numbering a document, right now just one anecdote, are both things a team could go test properly.
[S] I'd want that tested a lot more rigorously before leaning on it, one Bible passage is not an evaluation. But I agree the direction is a reasonable bet.
[G] If you remember one number from this paper, make it the split ones. One hundred percent birth-date retrieval, on the very same model that manages just twenty point four percent on the year alone. Zero percent full-name inverse search, across every ordinary configuration tested. Perfect storage and usable knowledge are not the same thing, and this paper proves that with a dataset built specifically to prove it.
[O] Mine's the forward-looking read. None of these failures look fundamental in the sense of unfixable, chain of thought helps enormously when it's actually used, reversed training data recovers inverse search on the columns that match. The lever exists. Somebody just has to pull it at production scale.
[S] Mine's the caution flag. "Inherent to language models" is proven, rigorously, inside a synthetic sandbox the authors built specifically to prove it. Its extension to GPT-4 on the real internet is consistent and real, but corroborated, not controlled, by the paper's own admission. Don't let the word "inherent" travel further than the evidence does.
[O] For the figures, the full breakdown by result number, and our written take, head to litsearch dot darvinyi dot com and find this paper under its slug. Thanks for listening.
